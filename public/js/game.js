function {
	// une fonction qui recupaire a chaque tour des info divisé en hero, adv, action, map
	// Initialisation d'un hero par default et création de l'objet qui sera utilisé sur le serv

	// structure personnage principale servira a faire des classe hérité pour les mosntre et hero
	var hero = {
		mp: 3,
		ap: 10,
		hp: 60,
		// caract ?
		stuff: {
			
		}
		// tmp calculer a partire des cac + arme
		dmg_c: 10,
		dmg_m: 7,
		dmg_d: 8,
		armor: 10,
	}
}